<UserControl x:Class="LingoIsland.Present.ScreenCapturePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- 版面統一（#182）：兩子頁籤【獲得】（擷取控制＋快速鍵）／【內容】（左清單｜右放大預覽）。以可見性切換 -->
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- 子頁籤：內容 / 獲得（USR：先內容再獲得） -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
      <RadioButton x:Name="ShotTabContent" Style="{StaticResource SubTab}" GroupName="ShotTab" IsChecked="True" Content="內容"/>
      <RadioButton x:Name="ShotTabAcquire" Style="{StaticResource SubTab}" GroupName="ShotTab" Content="獲得"/>
    </StackPanel>

    <Grid Grid.Row="1">
      <!-- 【獲得】擷取控制（按鈕＋快速鍵設定） -->
      <Border x:Name="ShotAcquirePane" Style="{StaticResource Card}" Padding="12" VerticalAlignment="Top" Visibility="Collapsed">
        <StackPanel>
          <TextBlock Text="螢幕擷取" FontWeight="Bold" FontSize="14" Foreground="{StaticResource PinkText}" Margin="0,0,0,8"/>
          <WrapPanel Orientation="Horizontal">
            <Button x:Name="CaptureScreenBtn" Content="擷取螢幕" Style="{StaticResource GhostButton}" Margin="0,0,16,4"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,4">
              <TextBlock Text="快捷鍵：" VerticalAlignment="Center" Foreground="{StaticResource PinkText}" Margin="0,0,4,0"/>
              <TextBlock x:Name="HotkeyStatus" MinWidth="80" VerticalAlignment="Center"
                         Foreground="{StaticResource PinkText}" FontWeight="SemiBold"/>
              <Button x:Name="ChangeHotkeyBtn" Content="變更" Style="{StaticResource GhostButton}" Margin="8,0,0,0"/>
            </StackPanel>
          </WrapPanel>
          <TextBlock Text="擷取螢幕（按鈕或快捷鍵）；擷取的截圖會顯示於「內容」子頁籤。"
                     Foreground="{StaticResource PinkSub}" FontSize="12" TextWrapping="Wrap" Margin="0,10,0,0"/>
        </StackPanel>
      </Border>

      <!-- 【內容】左＝截圖清單（Clear all＋theme 篩選；右鍵/Delete 刪）｜右＝放大預覽 -->
      <Grid x:Name="ShotContentPane">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="260" MinWidth="200"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*" MinWidth="240"/>
        </Grid.ColumnDefinitions>

        <!-- 左：Screenshots＋Clear all（置頂）＋依 theme 篩選＋清單 -->
        <Grid Grid.Column="0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid Grid.Row="0">
            <TextBlock Text="截圖" FontWeight="Bold" FontSize="13"
                       Foreground="{StaticResource PinkText}" VerticalAlignment="Center"/>
            <Button x:Name="ClearShotsBtn" Content="全部清除" Style="{StaticResource GhostButton}"
                    HorizontalAlignment="Right"/>
          </Grid>
          <!-- 依 theme 篩選（圖文下拉，多媒體主題管理·B） -->
          <ComboBox x:Name="ShotThemeFilter" Grid.Row="1" Height="26" Margin="0,6,0,4"
                    VerticalContentAlignment="Center" ToolTip="依主題篩選截圖"/>
          <Border Grid.Row="2" Style="{StaticResource Card}">
            <ListBox x:Name="ShotList" BorderThickness="0" Background="Transparent" Margin="4"
                     HorizontalContentAlignment="Stretch" ToolTip="右鍵點按或按 Delete 鍵以移除"/>
          </Border>
        </Grid>

        <GridSplitter Grid.Column="1" Style="{StaticResource ColSplitter}"/>

        <!-- 右：內容區塊（所屬主題＋放大預覽） -->
        <Border Grid.Column="2" Style="{StaticResource Card}" Margin="6,0,0,0" Padding="6">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- 所屬主題（內容區塊上方；顯示/改指派選取截圖之主題，#173） -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4,2,4,6" VerticalAlignment="Center">
              <TextBlock Text="主題：" VerticalAlignment="Center" Foreground="{StaticResource PinkText}" Margin="0,0,6,0"/>
              <ComboBox x:Name="ShotThemePicker" MinWidth="180" Height="26" VerticalContentAlignment="Center"
                        IsEnabled="False" ToolTip="顯示／變更此截圖所屬的主題"/>
            </StackPanel>
            <Grid Grid.Row="1">
              <Image x:Name="ShotPreview" Stretch="Uniform" Margin="6"/>
              <TextBlock x:Name="ShotEmptyHint" Text="尚無截圖。擷取螢幕（按鈕或快捷鍵）即可儲存於此。"
                         Foreground="#9A6A82" FontSize="13" TextWrapping="Wrap" MaxWidth="260"
                         TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
          </Grid>
        </Border>
      </Grid>
    </Grid>
  </Grid>
</UserControl>
